// This is for cleaner code (e.g. no undeclared variables)
"use strict";

// Easter Egg 3
if (randInt(1, 100) == 1) {
    document.getElementById("secret-3").innerHTML = "After all, SP is future in<strong>sp</strong>ired.";
}